import React, { memo } from 'react';

const Home = memo(() => {
    return (
        <div>
            <h1 className="text-3xl font-bold underline">
            Hello World!</h1>
        </div>
    );
});

export default Home;